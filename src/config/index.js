import descs from './descs';
import routes from './routes';
import titles from './titles';
import swr from './swr';

const config = {
    routes,
    titles,
    descs,
    swr,
};

export default config;
